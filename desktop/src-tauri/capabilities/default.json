{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Drydock menu-bar shell. The tray drives all server interaction over HTTP from Rust, so only window management and the opener plugin are exposed to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "opener:default"
  ]
}
