# Dependencies
node_modules
.pnpm-store

# Build output
.next
out
.turbo
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Test / coverage
coverage

# Environment & secrets
.env
.env.*
!.env.example

# Local application data (SQLite DB with repo data & tokens, backups)
data
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea
.vscode

# Tauri desktop shell (Rust). Build output and codegen are regenerated; the
# crate sources, Cargo.lock, and icons are committed. See desktop/README.md.
desktop/src-tauri/target
desktop/src-tauri/gen

# Local-only files (not part of the public project)
CLAUDE.md
.claude
DRYDOCK_SPEC.md
docs/superpowers
